services:
  suenmeow-web:
    restart: always
    environment:
      SUENMEOW_ENV: production
    volumes:
      - ./config:/app/config:ro
      - suenmeow_data:/app/data
      - ./logs:/app/logs

  suenmeow-worker:
    restart: always
    environment:
      SUENMEOW_ENV: production
    volumes:
      - ./config:/app/config:ro
      - suenmeow_data:/app/data
      - ./logs:/app/logs
